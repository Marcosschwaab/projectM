<p><%= t("devise.mailer.hello") %><%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t("devise.mailer.email_changing") %> <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p><%= t("devise.mailer.email_changed") %> <%= @resource.email %>.</p>
<% end %>
