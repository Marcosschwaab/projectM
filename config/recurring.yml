production:
  task_recurrence:
    class: TaskRecurrenceJob
    schedule: every day at 3:00am
    description: "Creates next instances of recurring tasks"

development:
  task_recurrence:
    class: TaskRecurrenceJob
    schedule: every 5 minutes
    description: "Creates next instances of recurring tasks (short interval for testing)"

test:
  task_recurrence:
    class: TaskRecurrenceJob
    schedule: every minute
    description: "Creates next instances of recurring tasks"
